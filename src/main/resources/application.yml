server:
  port: 9000
  servlet:
    context-path: /demo_elasticsearch
spring:
  name: demo-elasticsearch
  profiles:
    active: dev

##是否启动本地redis
redis:
  local:
    active: false
    maxheap: 200m
    port: 6379